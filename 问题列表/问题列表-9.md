## 问题列表-9（Redis 密码/DB 与 crob_agent 初始化）

背景：我已读取到你基础设施配置中：
- postgres：使用 `POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB`（来自 `D:\work\docker\infrastructure\envs\postgres\postgres.env`）
- redis：存在 `requirepass`（来自 `D:\work\docker\infrastructure\envs\redis\redis.conf`）

为避免信息不对齐，需要你确认以下事项：

1. Redis 是否启用了密码校验？
   - A. 是（使用 infrastructure 的 requirepass）
   - B. 否（你会关闭 requirepass 或提供无需密码的实例）

2. 本项目使用 Redis 的哪个 DB index？
   - A. 0
   - B. 1（推荐）
   - C. 2
   - D. 其它（你在对话里补充）

3. crob_agent 数据库与 schema 初始化怎么做？
   - A. 由我提供并执行 SQL（基于 `crob-ai-agent/deploy/postgres/init/*.sql`）在现有 postgres 上初始化（推荐）
   - B. 你手工创建 `crob_agent` 并初始化好 schema（我只对接连接串）
   - C. 暂不初始化（先确保服务能启动，后面再补）

