## 问题列表-7（前端构建异常的处理策略）

背景：当前 yudao-ui-admin-vue3（2026.04-snapshot）在生产构建后，浏览器启动阶段报错 `Module "" has been externalized ... .custom`，导致页面只显示 Logo，无法进入对话页。

1. 你希望我采用哪种修复策略？
   - A. 切换到稳定 tag（例如 v2.6.1 / v2025.12 / v2026.04）再做二次集成（推荐）
   - B. 保持当前 snapshot，新增 node polyfills 相关依赖/插件解决外部化问题（风险：依赖升级/锁文件变更较大）
   - C. 放弃 yudao-ui-admin-vue3，改用更轻量独立前端（不符合之前选择，但最快）

