## 问题列表-8（复用既有基础设施与本地启动对齐）

背景：你已在 `D:\work\docker\infrastructure` 启动了 postgres/redis（对外映射 5432/6379，容器名分别为 postgres/redis）。我们项目在测试阶段将直接复用这两个服务，并据此调整“本地启动联调方式”和“项目所需其余容器（如 MinIO 等）”的编排。

1. 本项目应当连接你现有的 postgres 的哪个数据库（DB name）？
   - A. 使用你当前 healthcheck 中的 `openclaw_meeting`（保持现状）
   - B. 新建并使用 `crob_agent`（推荐，避免污染其它项目）
   - C. 其它（你在对话里补充具体库名）

2. postgres 的账号密码我们使用哪一套？
   - A. 直接使用你 `D:\work\docker\infrastructure\envs\postgres\postgres.env` 里的账号密码（推荐）
   - B. 你给我一套新的账号密码（仅用于本项目）

3. redis 的连接方式我们按哪个来？
   - A. `redis://127.0.0.1:6379/0`（默认 db=0）
   - B. 指定其它 db（例如 db=1/2），避免与其它项目 key 冲突（推荐）
   - C. 有密码/ACL（你在对话里补充）

4. 本项目还需要的“其它环境”（MinIO / python-executor / web-admin 等）你希望怎么编排？
   - A. 在 `D:\work\docker\infrastructure` 的 compose 里追加 crob 相关服务（推荐，统一管理）
   - B. 保持 `crob-ai-agent/deploy/docker-compose.yml` 管理 crob 相关服务，但显式移除 postgres/redis，改为外部地址直连
   - C. 只要给我本地启动命令，其它都不需要 compose（纯本地）

